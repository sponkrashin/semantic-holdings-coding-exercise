import { Routes } from '@angular/router';

import { CpcSpendMonitoringComponent } from './cpc-spend-monitoring.component';

const routes: Routes = [
    { path: '', component: CpcSpendMonitoringComponent }
];

export default routes;
